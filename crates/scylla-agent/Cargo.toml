[package]
name = "scylla-agent"
version = "0.1.0"
edition = "2024"

[dependencies]
# broker (local path until published on crates.io)
hermes-broker-proto = { path = "C:/Users/sebas/RustroverProjects/hermes-rs/crates/proto" }
hermes-broker-client = { path = "C:/Users/sebas/RustroverProjects/hermes-rs/crates/client" }

# domain types (shared broker messages, no surrealdb)
scylla-core = { path = "../scylla-core", default-features = false, features = ["jobs"] }

# async
tokio = { workspace = true }
tokio-stream = "0.1"

# grpc
tonic = { workspace = true }

# serialization
serde = { workspace = true }
serde_json = { workspace = true }

# cli
clap = { workspace = true }

# error handling
thiserror = { workspace = true }
anyhow = { workspace = true }

# logging
tracing = { workspace = true }
tracing-subscriber = { workspace = true }

# time
chrono = { workspace = true }
