[package]
name = "scylla-auth"
version = "0.1.0"
edition = "2024"
license.workspace = true
repository.workspace = true

# The access model and its Cedar adapter. Sits below scylla-core (which is
# generic over the ports defined here) and depends only on the domain kernel.
[dependencies]
scylla-domain = { workspace = true }

async-trait = { workspace = true }
cedar-policy = { workspace = true }
chrono = { workspace = true }
derive_more = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }

[lints]
workspace = true
