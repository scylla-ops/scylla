# Examples

## Basic RBAC

Simple role-based access control.

```bash
cargo run --example basic_rbac
```

Shows admin vs user roles with different permissions.

## Multi-Tenant

RBAC with domains for multi-tenant applications.

```bash
cargo run --example multi_tenant
```

Demonstrates how users can have different roles in different organizations with proper isolation.
