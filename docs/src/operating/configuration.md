# Configuration

> 🚧 *Chapter in progress.*

The control plane is configured by a TOML file (`--config`) plus environment overrides. Full key list in the [Configuration reference](../reference/configuration.md).

## Config files

<!-- local.toml / docker.toml / prod.toml under crates/scylla-control-plane/config/. -->

## Sections

<!-- [grpc], [database], [cors], [bootstrap], [secrets]. -->

## Environment overrides

<!-- DATABASE_URL, RUST_LOG; keeping secrets out of the image. -->
