# Configuration reference

> 🚧 *Chapter in progress.*

Every configuration knob, in one place.

## Control-plane config (TOML)

<!-- Table: section.key | type | default | meaning — [grpc] [database] [cors] [bootstrap] [secrets]. -->

## Environment variables

<!-- DATABASE_URL, RUST_LOG, POSTGRES_*, VITE_API_URL. -->

## Agent CLI flags

<!-- Table from scylla-agent/src/config.rs: control-plane-url, app-id, app-secret, workspace-root, publish-buffer-size, max-reconnect-attempts, reconnect-backoff-secs, keep-workspace. -->
