# Triggers

> 🚧 *Chapter in progress.*

Triggers start pipelines automatically — on a schedule or from an inbound webhook.

## Cron triggers

<!-- 5-field Vixie cron (UTC), croner-backed. Spec + next-occurrence. -->

## Webhook triggers

<!-- POST /webhooks/{trigger_id} on port 8088; HMAC-SHA256 signature verification. -->

## Trigger inputs & deliveries

<!-- Trigger input payload; delivery records. -->
