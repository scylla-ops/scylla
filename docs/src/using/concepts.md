# Core concepts

> 🚧 *Chapter in progress.*

The handful of terms you need before using Scylla. Full definitions live in the [Glossary](../reference/glossary.md).

## Organizations & projects

<!-- Org = top-level tenant; Project = unit of work inside an org. -->

## Pipelines & nodes

<!-- Pipeline = DAG blueprint; Node = one step (command/args/deps). -->

## Jobs

<!-- Job = one execution of a pipeline; per-node state + timestamps. -->

## Apps & agents

<!-- App = machine principal; agent = App running the worker binary. -->

## Users & access

<!-- Membership + roles/grants, brief pointer to the access chapter. -->
