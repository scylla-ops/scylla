[package]
name = "application"
version = "0.1.0"
edition = "2024"

[dependencies]
domain = { path = "../domain" }
chrono = "0.4"
uuid = { version = "1", features = ["v4"] }
derive_more = { version = "2.1.1", features = ["constructor"] }
